---
const galleryItems = [
  {
    id: 1,
    title: "Antique Royal Choker",
    category: "Heritage Gold",
    desc: "22K hallmarked antique finish gold choker adorned with intricate filigree and delicate bead drops.",
    color: "from-amber-950/40 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <path d="M 20,30 Q 50,65 80,30" />
      <path d="M 25,30 Q 50,72 75,30" stroke-dasharray="1 2" />
      <path d="M 30,30 Q 50,80 70,30" />
      <circle cx="50" cy="74" r="3.5" fill="currentColor" />
      <circle cx="43" cy="69" r="2.5" fill="currentColor" />
      <circle cx="57" cy="69" r="2.5" fill="currentColor" />
      <circle cx="36" cy="60" r="2" fill="currentColor" />
      <circle cx="64" cy="60" r="2" fill="currentColor" />
      <circle cx="50" cy="46" r="3" fill="currentColor" />
    </svg>`
  },
  {
    id: 2,
    title: "Solitaire Diamond Ring",
    category: "Brilliant Diamonds",
    desc: "Round brilliant cut solitaire diamond claw-set in an elegant 18K white gold band.",
    color: "from-blue-950/40 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <circle cx="50" cy="55" r="20" />
      <path d="M 50,35 L 43,26 L 50,18 L 57,26 Z" />
      <line x1="50" y1="18" x2="50" y2="35" />
      <line x1="43" y1="26" x2="57" y2="26" />
      <circle cx="50" cy="26" r="1.5" fill="currentColor" />
    </svg>`
  },
  {
    id: 3,
    title: "Heritage Temple Jhumkas",
    category: "Heritage Gold",
    desc: "Intricately carved Laxmi motifs in temple gold hanging earrings with ruby accents.",
    color: "from-emerald-950/40 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <!-- Stud -->
      <circle cx="50" cy="25" r="8" />
      <circle cx="50" cy="25" r="3" fill="currentColor" />
      <!-- Hanging Bell -->
      <path d="M 38,52 C 38,40, 62,40, 62,52 L 62,58 L 38,58 Z" />
      <!-- Connecting loop -->
      <line x1="50" y1="33" x2="50" y2="44" />
      <!-- Hanging little bells -->
      <circle cx="42" cy="64" r="1.5" fill="currentColor" />
      <circle cx="50" cy="65" r="1.5" fill="currentColor" />
      <circle cx="58" cy="64" r="1.5" fill="currentColor" />
    </svg>`
  },
  {
    id: 4,
    title: "Royal Kundan Kada",
    category: "Bridal Couture",
    desc: "Heavy gold bangle encrusted with glass kundan stones and detailed red-green meenakari artwork on the inner rim.",
    color: "from-rose-950/40 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <!-- Outer circle -->
      <circle cx="50" cy="50" r="32" />
      <!-- Inner circle -->
      <circle cx="50" cy="50" r="24" />
      <!-- Kundan settings around ring -->
      <circle cx="50" cy="22" r="3.5" fill="currentColor" />
      <circle cx="50" cy="78" r="3.5" fill="currentColor" />
      <circle cx="22" cy="50" r="3.5" fill="currentColor" />
      <circle cx="78" cy="50" r="3.5" fill="currentColor" />
      <circle cx="30" cy="30" r="3.5" fill="currentColor" />
      <circle cx="70" cy="70" r="3.5" fill="currentColor" />
      <circle cx="30" cy="70" r="3.5" fill="currentColor" />
      <circle cx="70" cy="30" r="3.5" fill="currentColor" />
    </svg>`
  },
  {
    id: 5,
    title: "Classic Mangalsutra",
    category: "Heritage Gold",
    desc: "A gold pendant with modern geometry joined by traditional black bead chains.",
    color: "from-charcoal-800 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <!-- Chains -->
      <path d="M 15,30 Q 30,50 42,60" stroke-dasharray="2 2" />
      <path d="M 85,30 Q 70,50 58,60" stroke-dasharray="2 2" />
      <!-- Center Gold pendant -->
      <rect x="42" y="55" width="16" height="16" rx="2" fill="currentColor" transform="rotate(45 50 63)" />
      <circle cx="50" cy="63" r="3" fill="charcoal-900" />
      <circle cx="50" cy="78" r="2.5" fill="currentColor" />
    </svg>`
  },
  {
    id: 6,
    title: "Teardrop Diamond Necklace",
    category: "Brilliant Diamonds",
    desc: "Rows of round cut pave diamonds descending to a magnificent pear-shaped drop emerald.",
    color: "from-teal-950/40 to-charcoal-900",
    svg: `<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <!-- V shape neck chain -->
      <path d="M 20,25 L 50,70 L 80,25" />
      <path d="M 24,25 L 50,66 L 76,25" stroke-dasharray="1 1.5" />
      <!-- Hanging tear drop -->
      <path d="M 50,70 C 47,75, 47,80, 50,85 C 53,80, 53,75, 50,70 Z" fill="currentColor" />
      <circle cx="50" cy="62" r="2" fill="currentColor" />
    </svg>`
  }
];
---

<section id="gallery" class="py-24 bg-charcoal-900 relative">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Header -->
    <div class="flex flex-col md:flex-row justify-between items-end gap-6 mb-16">
      <div class="space-y-4">
        <span class="text-xs font-semibold tracking-[0.2em] text-gold-400 uppercase">Interactive Showroom</span>
        <h2 class="text-3xl sm:text-4xl font-serif font-bold text-white">Artisanal Gallery</h2>
        <div class="h-1 w-20 bg-gold-400 rounded-full"></div>
      </div>
      <p class="text-sm text-charcoal-400 max-w-md font-sans leading-relaxed">
        Click on any piece to inspect details, composition metrics, and custom order guidelines inside our digital lightbox.
      </p>
    </div>

    <!-- Gallery Grid (Masonry styling) -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {galleryItems.map((item) => (
        <button 
          type="button" 
          class="gallery-item group text-left w-full rounded-3xl overflow-hidden glass-panel border border-gold-400/10 transition-all duration-300 hover:border-gold-400/40 hover:-translate-y-1 hover:shadow-gold-glow flex flex-col cursor-pointer"
          data-id={item.id}
          data-title={item.title}
          data-category={item.category}
          data-desc={item.desc}
        >
          <!-- SVG Container -->
          <div class={`h-64 bg-gradient-to-br ${item.color} flex items-center justify-center relative overflow-hidden`}>
            <!-- Accent glow -->
            <div class="absolute inset-0 bg-gold-400/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <!-- Render the raw SVG string safely -->
            <div class="w-32 h-32 text-gold-400/60 group-hover:text-gold-400 group-hover:scale-105 transition-all duration-300" set:html={item.svg}></div>
            <span class="absolute top-4 right-4 px-2.5 py-1 rounded-full bg-charcoal-950/80 border border-gold-400/20 text-[9px] font-bold uppercase tracking-widest text-gold-300">
              {item.category}
            </span>
          </div>

          <!-- Bottom Info -->
          <div class="p-6 space-y-2 flex-grow bg-charcoal-950/40">
            <h3 class="font-serif text-lg font-bold text-white group-hover:text-gold-400 transition-colors duration-200">{item.title}</h3>
            <p class="text-xs text-charcoal-400 line-clamp-2 leading-relaxed">{item.desc}</p>
            <span class="text-[10px] font-bold uppercase tracking-wider text-gold-500 group-hover:text-gold-400 flex items-center gap-1.5 pt-2">
              Inspect Piece
              <svg class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" /></svg>
            </span>
          </div>
        </button>
      ))}
    </div>
  </div>

  <!-- Lightbox Modal -->
  <div id="lightbox" class="fixed inset-0 z-50 hidden items-center justify-center p-4 bg-charcoal-950/90 backdrop-blur-md transition-opacity duration-300" role="dialog" aria-modal="true" aria-labelledby="lightbox-title">
    <div class="absolute inset-0 cursor-pointer" id="lightbox-overlay"></div>
    
    <div class="relative glass-panel rounded-3xl max-w-2xl w-full overflow-hidden z-10 flex flex-col md:flex-row shadow-gold-glow-lg border border-gold-400/20 max-h-[90vh]">
      <!-- Close Button -->
      <button id="lightbox-close" type="button" class="absolute top-4 right-4 p-2 text-charcoal-400 hover:text-white rounded-full bg-charcoal-900/60 focus:outline-none focus:ring-2 focus:ring-gold-400" aria-label="Close details">
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>

      <!-- SVG Left Display -->
      <div id="lightbox-visual" class="h-64 md:h-auto md:w-1/2 bg-charcoal-900 flex items-center justify-center p-8 border-b md:border-b-0 md:border-r border-charcoal-800">
        <!-- SVG injected dynamically -->
      </div>

      <!-- Info Right Content -->
      <div class="p-8 md:w-1/2 flex flex-col justify-between overflow-y-auto">
        <div class="space-y-4">
          <span id="lightbox-category" class="text-[10px] font-bold uppercase tracking-widest text-gold-400">Category</span>
          <h3 id="lightbox-title" class="font-serif text-2xl font-bold text-white">Title</h3>
          <p id="lightbox-desc" class="text-sm text-charcoal-300 leading-relaxed font-sans">Description</p>
        </div>

        <div class="pt-8 border-t border-charcoal-800 space-y-4 mt-6">
          <div class="flex justify-between items-center text-xs text-charcoal-400">
            <span>Purity Standards:</span>
            <span class="text-gold-400 font-semibold">22K / 18K Hallmarked</span>
          </div>
          <a href="https://wa.me/919635675556" target="_blank" rel="noopener noreferrer" class="block w-full text-center py-3 rounded-full bg-gold-400 hover:bg-gold-300 text-charcoal-950 font-semibold tracking-wider text-xs uppercase transition-colors">
            Inquire About Design
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Dynamic lookup for visual SVGs
  const svgMap = {
    1: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <path d="M 20,30 Q 50,65 80,30" />
      <path d="M 25,30 Q 50,72 75,30" stroke-dasharray="1 2" />
      <path d="M 30,30 Q 50,80 70,30" />
      <circle cx="50" cy="74" r="3.5" fill="currentColor" />
      <circle cx="43" cy="69" r="2.5" fill="currentColor" />
      <circle cx="57" cy="69" r="2.5" fill="currentColor" />
      <circle cx="36" cy="60" r="2" fill="currentColor" />
      <circle cx="64" cy="60" r="2" fill="currentColor" />
      <circle cx="50" cy="46" r="3" fill="currentColor" />
    </svg>`,
    2: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <circle cx="50" cy="55" r="20" />
      <path d="M 50,35 L 43,26 L 50,18 L 57,26 Z" />
      <line x1="50" y1="18" x2="50" y2="35" />
      <line x1="43" y1="26" x2="57" y2="26" />
      <circle cx="50" cy="26" r="1.5" fill="currentColor" />
    </svg>`,
    3: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <circle cx="50" cy="25" r="8" />
      <circle cx="50" cy="25" r="3" fill="currentColor" />
      <path d="M 38,52 C 38,40, 62,40, 62,52 L 62,58 L 38,58 Z" />
      <line x1="50" y1="33" x2="50" y2="44" />
      <circle cx="42" cy="64" r="1.5" fill="currentColor" />
      <circle cx="50" cy="65" r="1.5" fill="currentColor" />
      <circle cx="58" cy="64" r="1.5" fill="currentColor" />
    </svg>`,
    4: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <circle cx="50" cy="50" r="32" />
      <circle cx="50" cy="50" r="24" />
      <circle cx="50" cy="22" r="3.5" fill="currentColor" />
      <circle cx="50" cy="78" r="3.5" fill="currentColor" />
      <circle cx="22" cy="50" r="3.5" fill="currentColor" />
      <circle cx="78" cy="50" r="3.5" fill="currentColor" />
      <circle cx="30" cy="30" r="3.5" fill="currentColor" />
      <circle cx="70" cy="70" r="3.5" fill="currentColor" />
      <circle cx="30" cy="70" r="3.5" fill="currentColor" />
      <circle cx="70" cy="30" r="3.5" fill="currentColor" />
    </svg>`,
    5: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <path d="M 15,30 Q 30,50 42,60" stroke-dasharray="2 2" />
      <path d="M 85,30 Q 70,50 58,60" stroke-dasharray="2 2" />
      <rect x="42" y="55" width="16" height="16" rx="2" fill="currentColor" transform="rotate(45 50 63)" />
      <circle cx="50" cy="63" r="3" fill="charcoal-900" />
      <circle cx="50" cy="78" r="2.5" fill="currentColor" />
    </svg>`,
    6: `<svg class="w-48 h-48 text-gold-400" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.2">
      <path d="M 20,25 L 50,70 L 80,25" />
      <path d="M 24,25 L 50,66 L 76,25" stroke-dasharray="1 1.5" />
      <path d="M 50,70 C 47,75, 47,80, 50,85 C 53,80, 53,75, 50,70 Z" fill="currentColor" />
      <circle cx="50" cy="62" r="2" fill="currentColor" />
    </svg>`
  };

  const lightbox = document.getElementById('lightbox');
  const lightboxOverlay = document.getElementById('lightbox-overlay');
  const lightboxClose = document.getElementById('lightbox-close');
  const lightboxVisual = document.getElementById('lightbox-visual');
  const lightboxCategory = document.getElementById('lightbox-category');
  const lightboxTitle = document.getElementById('lightbox-title');
  const lightboxDesc = document.getElementById('lightbox-desc');
  const galleryItems = document.querySelectorAll('.gallery-item');

  const openLightbox = (el: HTMLElement) => {
    const id = el.getAttribute('data-id');
    const title = el.getAttribute('data-title');
    const category = el.getAttribute('data-category');
    const desc = el.getAttribute('data-desc');

    if (lightbox && lightboxVisual && lightboxCategory && lightboxTitle && lightboxDesc) {
      // Inject values
      lightboxCategory.textContent = category;
      lightboxTitle.textContent = title;
      lightboxDesc.textContent = desc;
      lightboxVisual.innerHTML = svgMap[id as unknown as keyof typeof svgMap] || '';
      
      // Show lightbox
      lightbox.classList.remove('hidden');
      lightbox.classList.add('flex');
      document.body.style.overflow = 'hidden'; // Stop page scrolling
      
      // Accessibility focus trap setup
      lightboxClose?.focus();
    }
  };

  const closeLightbox = () => {
    if (lightbox) {
      lightbox.classList.add('hidden');
      lightbox.classList.remove('flex');
      document.body.style.overflow = ''; // Resume page scrolling
    }
  };

  galleryItems.forEach(item => {
    item.addEventListener('click', () => openLightbox(item as HTMLElement));
  });

  lightboxClose?.addEventListener('click', closeLightbox);
  lightboxOverlay?.addEventListener('click', closeLightbox);

  // Keyboard navigation escape support
  window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      closeLightbox();
    }
  });
</script>
